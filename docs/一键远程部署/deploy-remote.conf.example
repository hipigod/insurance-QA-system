################################################################################
# 远程自动部署配置文件
# 使用方法：
#   1. 复制此文件为 deploy-remote.conf
#   2. 填写你的实际配置
#   3. 执行 bash deploy-remote.sh
################################################################################

#==============================================================================
# 服务器连接配置
#==============================================================================

# 服务器地址（域名或IP）
SERVER_HOST="..."

# SSH用户名
SERVER_USER="root"

# SSH密码（如果使用密钥认证，可以留空）
# 注意：脚本优先使用SSH密钥，如果没有配置密钥才会使用密码
SERVER_PASSWORD=""

# SSH端口（默认22，如果修改过请填写）
# SERVER_PORT="22"

#==============================================================================
# 项目配置
#==============================================================================

# 项目在服务器上的路径
PROJECT_PATH="/xxx/xxx"

# Git分支名称
GIT_BRANCH="xxxxxx"

#==============================================================================
# 部署选项（可选）
#==============================================================================

# 是否在部署前备份（yes/no）
# BACKUP_BEFORE_DEPLOY="no"

# 是否在部署后运行健康检查（yes/no）
# HEALTH_CHECK_ENABLED="yes"

# 是否显示详细日志（yes/no）
# VERBOSE_LOGGING="no"

#==============================================================================
# 高级选项（一般不需要修改）
#==============================================================================

# Docker Compose命令（如果不是标准安装，可能需要指定完整路径）
# DOCKER_COMPOSE_CMD="docker compose"

# Git远程仓库名称（origin）
# GIT_REMOTE="origin"

#==============================================================================
# 说明
#==============================================================================

# 1. SSH认证方式：
#    - 推荐：使用SSH密钥（更安全、无需输入密码）
#      生成密钥：ssh-keygen -t rsa -b 4096
#      复制公钥到服务器：ssh-copy-id root@8.138.18.77
#
#    - 备选：使用密码（需要每次输入，或使用sshpass）
#      安装sshpass：apt install sshpass (Ubuntu) 或 brew install sshpass (Mac)
#
# 2. 配置文件示例：
#    最小配置只需要填写 SERVER_HOST, SERVER_USER, PROJECT_PATH, GIT_BRANCH
#
# 3. 安全建议：
#    - 不要将 deploy-remote.conf 提交到Git仓库
#    - 已添加到 .gitignore
#    - 只分享 .example 文件
